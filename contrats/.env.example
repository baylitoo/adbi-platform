# ADBI Contrats — variables d'environnement.
# Sans ce fichier (copié en .env), les mêmes réglages (hors DATABASE_URL)
# restent saisissables depuis l'écran Paramètres de l'application (écrits
# dans data/secrets.json, hors git). Une variable d'environnement est
# prioritaire sur secrets.json.

PORT=4100

# REQUISE — connexion PostgreSQL (issue #14). Le serveur refuse de démarrer
# sans elle. Le schéma (lib/schema.sql) est appliqué automatiquement
# (CREATE TABLE IF NOT EXISTS, idempotent) au premier démarrage.
#   ex. : postgresql://adbi:<mot-de-passe>@localhost:5432/adbi_contrats
DATABASE_URL=

# Code d'accès à l'écran Paramètres. Sans cette variable, un code par défaut
# est écrit dans data/code-parametres.txt au premier démarrage — à définir
# explicitement avant tout déploiement accessible depuis Internet.
ADBI_CODE_PARAMETRES=

# ── Contrôle d'accès au SERVICE (#245) ────────────────────────────────────────
# À ne pas confondre avec ADBI_CODE_PARAMETRES juste au-dessus. Les deux verrous
# coexistent et répondent à des questions différentes :
#   ADBI_CODE_PARAMETRES  « as-tu le code de cet ÉCRAN ? »  — Paramètres seuls
#   ADBI_AUTH             « QUI es-tu ? »                   — le service entier
# Avant #245, seul le premier existait : tout le reste du service (contrats,
# signatures, documents générés) était accessible à qui atteignait l'URL.
#
# ADBI_AUTH absent/off = comportement d'avant, à l'octet : accès libre sous
# l'identité locale. Posé à "on", contrats exige une session émise par
# cv-parser — seul émetteur d'identité — et VÉRIFIÉE ici avec le même secret.
# contrats ne signe jamais de jeton.
ADBI_AUTH=
# Exactement la même valeur que cv-parser, sinon toute session est refusée.
# ADBI_AUTH=on SANS ce secret = refus de tout, jamais passage libre : une
# variable oubliée doit être une panne visible, pas une ouverture silencieuse.
ADBI_JWT_SECRET=
# URL publique du hub, ex. https://hub.exemple.test
# contrats est affiché dans une <iframe> du hub : un visiteur sans session
# reçoit une page qui renvoie le NIVEAU SUPÉRIEUR vers le hub, seul capable de
# renouveler la session (une redirection sur place ferait un cadre mort au bout
# d'une heure). Vide : réponse 401 en clair, sans cible de reconnexion.
ADBI_FACTORY_URL=
#
# Deux chemins restent publics quoi qu'il arrive :
#   /api/sante          sonde du conteneur — un 401 ferait redémarrer en boucle
#   /webhooks/signature rappel entrant de Yousign/Zoho, authentifié par HMAC
#                       avec YOUSIGN_WEBHOOK_SECRET / ZOHO_WEBHOOK_SECRET

# Recherche société : gouv.fr est utilisé par défaut, sans clé.
# Renseigner l'une de ces deux clés pour basculer sur Pappers ou INSEE Sirene.
PAPPERS_API_KEY=
INSEE_API_KEY=

# Envoi des invitations de signature (sans SMTP : repli sur liens/mailto manuels).
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Signature électronique — Yousign (connecteur par défaut).
YOUSIGN_API_KEY=
YOUSIGN_MODE=sandbox
YOUSIGN_WEBHOOK_SECRET=

# Signature électronique — Zoho Sign (alternative, région UE par défaut).
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_REGION=eu
ZOHO_WEBHOOK_SECRET=

# Extraction Kbis, URSSAF, RIB et attestation de régularité fiscale via le
# bridge DocIE partagé (document-parsing/bridge/) — issues #153, #170 et #215.
# Désactivée par défaut : analyse 100% locale (pdf-parse + tesseract.js, sans
# envoi externe, lib/docanalyze.js) inchangée. Quatre pièces, deux voies (voir
# lib/docie-extraction.js) :
#   - Kbis   : voie agent, le document part entier, l'agent résout son schéma
#              PAR NOM -> DOCIE_AGENT_KBIS ci-dessous, et un schéma enregistré
#              côté Studio DocIE. Choisi par type d'entrée dès qu'un modèle du
#              catalogue est configuré (#194) : un PDF à couche texte passe par
#              la voie texte (document-parsing/schemas/kbis.schema.json).
#   - URSSAF : voie texte, la couche texte du PDF est lue localement puis
#              envoyée avec la DÉFINITION du schéma dans le corps de la requête
#              (document-parsing/schemas/urssaf.schema.json). Rien à
#              enregistrer côté Studio, AUCUN agent à configurer. Une
#              attestation scannée (image, ou PDF dont une page est sans texte)
#              n'est jamais envoyée : analyse locale, avec un avertissement.
#   - RIB    : voie texte, même mécanique (document-parsing/schemas/
#              rib.schema.json) ; IBAN et BIC contrôlés (lib/iban-bic.js).
#   - FISCALE: attestation de régularité fiscale, voie texte, même mécanique
#              (document-parsing/schemas/fiscale.schema.json), #215. ATTENTION :
#              la checklist n'a pas encore de bouton d'analyse pour cette pièce
#              (le bouton OCR est posé sous `dateField`, qu'elle n'a pas) —
#              atteignable par POST /api/document/analyze seulement.
# Les trois autres pièces restent toujours locales, flag ou pas. Coordonnées et
# informations spécifiques n'ont ni schéma ni mapping : ce sont des champs de
# saisie, pas des documents. La CNI, elle, a bien un schéma et un mapping, mais
# passe par la voie VISION du catalogue (tâche "cni"), avec le contrôle des
# chiffres de la MRZ pour prérequis (lib/mrz.js). Sur échec DocIE (config
# manquante, timeout, erreur upstream...), repli automatique sur l'analyse
# locale.
#
# Même flag pour le pré-remplissage de l'import de contrat depuis un document
# (POST /api/contracts/importer/extraire, schéma DocIE "contract" — voir
# lib/docie-contract-import.js) : pas de repli local ici (aucune analyse
# locale équivalente à 19 champs structurés), flag off = fonctionnalité
# simplement absente, saisie manuelle comme aujourd'hui.
# Vide : l'analyse des pièces passe par DocIE d'office dès qu'un modèle texte
# par défaut y est prêt, repli local sinon ; false : toujours locale.
DOCIE_EXTRACTION_ENABLED=
# Racine API DocIE (sans /v1), clé serveur et agents Kbis/Contrat — requis
# uniquement si DOCIE_EXTRACTION_ENABLED=true. Voir document-parsing/bridge/README.md.
DOCIE_BASE_URL=
DOCIE_API_KEY=
DOCIE_AGENT_KBIS=
# Agent DocIE pour le schéma "contract" (pré-remplissage de l'import) — à
# configurer côté DocIE, requis uniquement pour /api/contracts/importer/extraire.
DOCIE_AGENT_CONTRACT=
# Modèles proposés par action (#194, document-parsing/models/catalogue.json) :
# identifiant réel de chaque modèle sur la voie texte, une référence
# `store:<nom>` lue sur GET /v1/serving/store. Un modèle laissé vide n'est pas
# proposé. Tous vides : comportement d'avant (contrat par DOCIE_AGENT_CONTRACT,
# URSSAF et RIB sans sélecteur, avec leur repli local).
# Dès qu'un modèle est configuré, un sélecteur s'affiche à côté de « Pré-remplir
# depuis le PDF » (contrat : NuExtract3 par défaut, LFM2.5 2.6B seulement pour
# un contrat de 800 lignes non vides au plus) et des analyses URSSAF et RIB
# (LFM2.5 2.6B par défaut, 350M en alternative ; pour le RIB, une lecture du
# 350M sans IBAN et BIC contrôlés valides est affichée ⛔). Il est masqué s'il
# n'y a qu'un modèle.
# Un contrat passe alors par la voie texte (un contrat scanné est refusé,
# saisie manuelle), et un échec du modèle choisi s'affiche tel quel : pas de
# repli sur l'analyse locale.
DOCIE_MODELE_NUEXTRACT3=
DOCIE_MODELE_LFM25_2_6B=
DOCIE_MODELE_LFM25_350M=
# Kbis « choisi par type d'entrée » (#194). PDF à couche texte complète : voie
# texte, LFM2.5 2.6B par défaut, NuExtract3 en alternative (DOCIE_MODELE_* ci-
# dessus) ; sans choix, il n'y passe que si DOCIE_MODELE_LFM25_2_6B est
# renseigné, sinon DOCIE_AGENT_KBIS comme avant. Photo, scan ou page muette :
# vision, NuExtract3 seul, par l'agent ci-dessous (8 pages au plus, refus
# `limite` au-delà) ; un modèle de la seule voie texte y est refusé en `scan`.
# Vide = NuExtract3 non proposé en vision (Kbis sans choix : DOCIE_AGENT_KBIS).
DOCIE_AGENT_KBIS_NUEXTRACT3=
# DocIE joint en http:// sur un réseau privé de confiance (hors localhost) :
# le bridge refuse sans DOCIE_ALLOW_HTTP=true, et TOUTES les extractions
# DocIE de ce service (Kbis, URSSAF, import de contrat) échouent en
# « configuration » avant le moindre appel.
DOCIE_ALLOW_HTTP=
# Délai client d'une extraction DocIE, en secondes (1 à 3600, défaut 360).
DOCIE_TIMEOUT_SECONDS=360
# Pré-remplissages de contrat DocIE simultanés (tâche asynchrone, issue #196) ;
# les autres attendent en file (20 au plus, 503 au-delà). À aligner sur le
# `n_parallel` du modèle servi pour les contrats (liste retenue de #194) : 1
# pour NuExtract3 (défaut des contrats), 2 pour LFM2.5-2.6B. Au-delà, la requête
# admise attend dans llama-server et cette attente compte dans
# DOCIE_TIMEOUT_SECONDS. Absente ou vide -> 1 ; entier de 1 à 16, toute autre
# valeur empêche le démarrage. Même nom dans one-pager et cv-parser ; dans le
# docker-compose.yml racine : CONTRATS_EXTRACTION_MAX_CONCURRENT.
ADBI_EXTRACTION_MAX_CONCURRENT=1

# Délais (ms) des appels HTTP sortants vers les fournisseurs externes
# (Yousign, Zoho Sign, recherche société gouv.fr/Pappers/INSEE). Défauts :
# 15 s pour un appel API classique, 60 s pour l'envoi/téléchargement d'un
# document (PDF du contrat). À ajuster seulement si un fournisseur s'avère
# systématiquement plus lent que ces délais.
ADBI_DELAI_HTTP_MS=15000
ADBI_DELAI_UPLOAD_MS=60000

# OpenAI, modèle d'extraction EXTERNE (hors ADBI, #194) : clé non vide =
# « OpenAI rapide » et « OpenAI raisonnement » proposés en plus des modèles
# DocIE, jamais par défaut, pour contrat, Kbis, URSSAF, attestation fiscale et
# RIB (voie texte). Choix explicite seulement : jamais un repli, un échec
# s'affiche tel quel. PAS ENCORE LU par ce service : la PR de câblage à suivre
# l'ajoutera au sélecteur (document-parsing/bridge/openai-responses.js existe).
# Protection des données : sous-traitant externe, le TEXTE du document part chez
# OpenAI ; `store: false` (sinon 30 jours de conservation), mais journaux de
# surveillance des abus gardés jusqu'à 30 jours sauf accord ZDR ; traitement en
# UE (https://eu.api.openai.com) seulement sur accord d'OpenAI. Détail et tarifs
# lus le 2026-09-16 : voir le .env.example racine.
OPENAI_API_KEY=
# Racine sans /v1 (vide = https://api.openai.com).
OPENAI_BASE_URL=
# gpt-4.1-nano (vide = défaut) ou gpt-4.1-mini ; aucun raisonnement.
OPENAI_MODELE_RAPIDE=
# gpt-5-nano (vide = défaut, seul permis) ; reasoning.effort « low ».
OPENAI_MODELE_RAISONNEMENT=
# Secondes (1 à 3600) ; vide = DOCIE_TIMEOUT_SECONDS, sinon 360.
OPENAI_TIMEOUT_SECONDS=
