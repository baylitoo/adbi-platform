# ADBI Contrats — variables d'environnement optionnelles.
# Sans ce fichier (copié en .env), les mêmes réglages restent saisissables
# depuis l'écran Paramètres de l'application (écrits dans data/secrets.json,
# hors git). Une variable d'environnement est prioritaire sur secrets.json.

PORT=4100

# Recherche société : gouv.fr est utilisé par défaut, sans clé.
# Renseigner l'une de ces deux clés pour basculer sur Pappers ou INSEE Sirene.
PAPPERS_API_KEY=
INSEE_API_KEY=
