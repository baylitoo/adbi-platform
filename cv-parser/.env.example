# ADBI Parser — variables d'environnement optionnelles.

# Secret JWT (session utilisateur). Si absent, généré au premier lancement
# dans data/jwt_secret.txt et réutilisé ensuite — inutile de le fixer ici en
# développement. À fixer explicitement pour un déploiement multi-instance
# (sinon chaque instance signe avec un secret différent).
ADBI_JWT_SECRET=

# Passerelle d'inférence interne ADBI (extraction de CV) — compatible API
# OpenAI. Sans ADBI_LLM_BASE_URL, aucune IA n'est appelée : l'extraction
# locale Docling reste seule disponible (voir llm_cascade.py::chaine_defaut).
ADBI_LLM_BASE_URL=
ADBI_LLM_API_KEY=
ADBI_LLM_MODEL=
# Plusieurs modèles à essayer en cascade (séparés par des virgules) — prend
# le pas sur ADBI_LLM_MODEL si renseigné.
ADBI_LLM_MODELS=
