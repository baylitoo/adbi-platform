flask>=3.0.0
flask-cors>=4.0.0
docling>=2.0.0
requests>=2.31.0
pdfplumber>=0.10.0
pypdf>=4.0.0
python-docx>=1.1.0
PyJWT>=2.8.0
bcrypt>=4.0.0
pydantic>=2.0.0
# export_dossier.py (dossier de competences PDF/Word) importe `fitz` sans
# condition — manquait de ce fichier alors qu'app.py importe export_dossier
# au demarrage, ce qui faisait echouer le boot. Wheel abi3 (cp310-abi3)
# disponible pour Python 3.14 sur win_amd64 et manylinux_2_28 x86_64 :
# aucun compilateur requis, meme sur l'image Docker python:3.14-slim.
pymupdf>=1.24.0
# PostgreSQL (issue #15, PR A) — v3 sync, cv-parser est un WSGI sync (Flask),
# pas de pilote async necessaire. Variante [binary] : pas de libpq a
# installer sur l'hote/l'image Docker.
psycopg[binary]>=3.1
