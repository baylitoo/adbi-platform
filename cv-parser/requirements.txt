flask>=3.0.0
flask-cors>=4.0.0
docling>=2.0.0
requests>=2.31.0
pdfplumber>=0.10.0
pypdf>=4.0.0
python-docx>=1.1.0
PyJWT>=2.8.0
bcrypt>=4.0.0
pydantic>=2.0.0
# PostgreSQL (issue #15, PR A) — v3 sync, cv-parser est un WSGI sync (Flask),
# pas de pilote async necessaire. Variante [binary] : pas de libpq a
# installer sur l'hote/l'image Docker.
psycopg[binary]>=3.1
