# ADBI OnePager — variables d'environnement optionnelles.
PORT=4200

# PostgreSQL (voir lib/db.pg.js, lib/schema.sql) — provisionnée par le
# docker-compose.yml racine. Pas encore consommée par server.js (issue #16) :
# ne sert pour l'instant qu'à scripts/migrer-vers-postgres.js.
DATABASE_URL=
